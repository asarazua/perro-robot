# Documentación del package.json

```jsonc
{
  // Nombre del proyecto
  "name": "robot-server",

  // Versión inicial del proyecto
  "version": "1.0.0",

  // Dependencias necesarias para correr el servidor
  "dependencies": {
    // Express: framework para crear el servidor web
    "express": "^4.18.2",

    // Mongoose: librería para conectarse y trabajar con MongoDB
    "mongoose": "^7.5.0"
  }
}

